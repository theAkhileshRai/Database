Datatypes for Mongo DB

Text 
Boolean
Numbers: int32, NumberLong(int64),
 NumberDecimal(floating point), 
ObjectId,
ISODate, 
TimeStamp(mostly internal to mongodb),
EmbeddedDocuments,Array

in shell everything is stored as 64 bit float
